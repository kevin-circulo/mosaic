{
  "name": "rh-query-member",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "faker": "^5.5.3"
  },
  "devDependencies": {
    "serverless-offline": "^8.0.0",
    "serverless-offline-scheduler": "^0.5.0"
  }
}
